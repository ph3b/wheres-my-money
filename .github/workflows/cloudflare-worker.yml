name: Deploy
on:
  push:
    branches:
      - main

jobs:
  deploy-dev:
    uses: "./.github/workflows/actions/deploy-action.yml"
    with:
      target-env: dev
    secrets: inherit

  deploy-prod:
    uses: "./.github/workflows/actions/deploy-action.yml"
    with:
      target-env: prod
    secrets: inherit
